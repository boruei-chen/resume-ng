import { DateCountDirective } from './date-count.directive';

describe('DateCountDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCountDirective();
    expect(directive).toBeTruthy();
  });
});
